import numpy as np
class Table:
    def __init__(self) -> None:
        self.rows = 0
        self.x = np.array([])
        self.y = np.array([])
        self.z = np.array([])
        self.weight = np.array([])